import React from 'react';

import { FooterContainer } from './styles';

const Footer = ({ children }) => {
  
    return (

        <FooterContainer>
            {children}
        </FooterContainer>

    );

}

export default Footer;